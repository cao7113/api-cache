// https://orm.drizzle.team/docs/seed-overview

import { db } from "./sqlite-setup";
import { remoteResponsesTable } from "./schema";
import { seed } from "drizzle-seed";

// async function main() {
//   await seed(db, { remoteResponsesTable });
// }

// main();
